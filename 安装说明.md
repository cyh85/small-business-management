# 如何安装「横纵分析法」Skill

> 给学员看的一页纸。三步搞定,不需要会命令行。

## 方法一:让 AI 自己装(推荐)

### 第 1 步:下载仓库

打开 GitHub 页面,点右上角绿色 **Code → Download ZIP**:

```
https://github.com/cyh85/small-business-management
```

下载后解压到任意文件夹(比如桌面)。

### 第 2 步:告诉你的 AI 一句话

打开你正在用的 AI 工具(Claude / Cursor / OpenCode 都行),把下面这段话**直接复制粘贴**发给它:

```
请把我刚下载的 small-business-management 仓库里的 skills/hv-analysis 这个 skill 安装到你的 skills 目录。
如果你不确定自己的 skills 目录在哪,先检测一下:
- Claude:   ~/.claude/skills/            (Windows: %USERPROFILE%\.claude\skills\)
- Cursor:   ~/.cursor/skills/            (Windows: %USERPROFILE%\.cursor\skills\)
- OpenCode: ~/.config/opencode/skills/   (Windows: %USERPROFILE%\.config\opencode\skills\)
找到正确位置后,把整个 hv-analysis 文件夹复制过去。装完告诉我装到哪了。
```

### 第 3 步:验证

AI 装完后,你直接对它说:

```
用横纵分析法研究「小微企业的 AI 客服选型」
```

如果它开始联网搜索并产出结构化报告,说明安装成功 🎉

---

## 方法二:一行命令安装(给会用命令行的学员)

**Windows (PowerShell):**
```powershell
irm https://raw.githubusercontent.com/cyh85/small-business-management/main/install.ps1 | iex
```

**Mac / Linux (终端):**
```bash
curl -fsSL https://raw.githubusercontent.com/cyh85/small-business-management/main/install.sh | bash
```

---

## 常见问题

**Q: 我的 AI 工具打不开 skills 目录怎么办?**
A: 大多数 AI 工具(Claude Code、Cursor、OpenCode)都支持 skills 目录。如果不确定,用方法一,让 AI 自己找。

**Q: 装完之后找不到 skill?**
A: 重启一下 AI 工具,或者在对话里明确说"用横纵分析法",AI 才会触发这个 skill。

**Q: 我想更新到最新版?**
A: 重新下载 ZIP,再用方法一装一次(覆盖即可)。

**Q: 我用的不是上面三个工具?**
A: 任何支持"自定义 skills / 提示词 / 指令"的 AI 工具都能用。把 `skills/hv-analysis/SKILL.md` 的内容复制到你工具的"自定义指令"或"项目规则"里即可。
